<?xml version="1.0" encoding="utf-8"?>
<pt.it.porto.mydiabetes.ui.charts.BodyExpandFrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/frame"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@null"
    android:orientation="vertical"
    tools:context=".ui.fragments.ChartFragment">

    <lecho.lib.hellocharts.view.LineChartView
        android:id="@+id/chart"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/chart_drawable"
        android:padding="1dp" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/list_vals"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="5dp"
        android:layout_marginRight="5dp"
        android:layout_marginTop="100dp"
        android:background="@android:color/white"
        android:choiceMode="singleChoice"
        android:clipToPadding="false"
        android:elevation="2dp"
        android:paddingBottom="120dp"
        android:scrollbarStyle="outsideOverlay"
        android:scrollbars="vertical"
        tools:listitem="@layout/list_weight_row" />

    <include
        android:id="@+id/list_empty"
        layout="@layout/list_empty"
        android:visibility="gone"/>

</pt.it.porto.mydiabetes.ui.charts.BodyExpandFrameLayout>
